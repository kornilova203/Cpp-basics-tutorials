/*
 * 1. Создать переменную a типа int, присвоить ей 10
 * 2. Создать указатель на переменную типа int
 * 3. Присвоить указателю адрес переменной (операция взятия адреса: &a)
 * 4. С помощью указателя присвоить переменной 20. (операция разыменования: *pointer)
 * 5. Вывести значение переменной на экран, используя указатель
 * 6. Вывести значение переменной на экран, используя переменную
 */

#include <iostream>
using namespace std;

int main () {
    // твой код здесь

    return 0;
}
